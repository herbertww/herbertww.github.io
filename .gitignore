# Original full-size drops — the optimised web copies live in assets/img/
assets/*.png
assets/*.jpeg
assets/*.jpg
!assets/img/*.jpg

# Superseded by assets/video/runturfing-demo.mp4 (kept locally as the source)
assets/img/*.gif

# deploy
node_modules/
deploy/public/
demo-*.png
